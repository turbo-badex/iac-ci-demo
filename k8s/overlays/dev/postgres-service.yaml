apiVersion: v1
kind: Service
metadata:
  name: orders-postgres
  labels:
    app: orders-postgres
spec:
  type: ClusterIP
  selector:
    app: orders-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432